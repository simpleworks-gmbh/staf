package de.simpleworks.staf.commons.interfaces;

public interface IPojo {
	/**
	 * @brief method to validate the integrity of an instance
	 * @return {@param boolean} true if the instance is valid, false if not
	 */
	boolean validate();
}
